<%= form_with(model: revistum, local: true) do |form| %>
  <% if revistum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(revistum.errors.count, "error") %> prohibited this revistum from being saved:</h2>

      <ul>
        <% revistum.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :enologos_id %>
    <%= form.text_field :enologos_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
